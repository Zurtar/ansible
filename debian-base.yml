---
- name: Setup Debian Machine
  hosts: localhost
  become: true
  roles:
    - debian-base
  post_tasks:
    - name: Reboot
      ansible.builtin.command: shutdown -r +1
      async: 1
      poll: 0
