---
- name: Set hostname
  ansible.builtin.hostname:
    name: "{{ hostname }}"

- name: Create user directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: "{{ target_user }}"
    group: "{{ target_user }}"
    mode: "0755"
  loop:
    - "{{ target_home }}/.local/bin"
    - "{{ target_home }}/repos"
    - "{{ target_home }}/notes"

